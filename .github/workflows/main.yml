name: Build and Push Docker Image to ACR

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code from the repository
      - name: Checkout Code
        uses: actions/checkout@v3

      # Build the Docker image
      - name: Build Docker Image
        run: |
          docker build -t java-app ./Springboot-application-docker

      # Log in to Azure Container Registry (ACR)
      - name: Log in to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.ACR_LOGIN_SERVER }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      # Push Docker image to ACR
      - name: Push Docker Image to ACR
        run: |
          docker tag java-app ${{ secrets.ACR_LOGIN_SERVER }}/java-app:latest
          docker push ${{ secrets.ACR_LOGIN_SERVER }}/java-app:latest



# name: Build and Push Docker Image to ACR

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       # Checkout the code from the repository
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       # Build the Docker image
#       - name: Build Docker Image
#         run: |
#           # Adjust the build context to point to the directory where the Dockerfile is located
#           docker build -t java-app ./Springboot-application-docker

#       # Log in to Azure Container Registry (ACR)
#       - name: Log in to Azure Container Registry
#         uses: azure/docker-login@v1
#         with:
#           login-server: ${{ secrets.ACR_LOGIN_SERVER }}
#           username: ${{ secrets.ACR_USERNAME }}
#           password: ${{ secrets.ACR_PASSWORD }}

#       # Push Docker image to ACR
#       - name: Push Docker Image to ACR
#         run: |
#           docker tag java-app ${{ secrets.ACR_LOGIN_SERVER }}/java-app:latest
#           docker push ${{ secrets.ACR_LOGIN_SERVER }}/java-app:latest
